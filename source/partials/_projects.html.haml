.row
  .projects
    - data.projects.each do |project|
      .small-12.medium-4.large-4.columns
        %h4= project[:name]
        %P= project[:description]
        %P= project[:language]
        %a{href: "#{project[:github]}"}
          %button.btn.btn-default.btn-sm.pushred{type: "button"}
            =image_tag 'pushred.png', width: '30px'
